from dataclasses import dataclass
from pathlib import Path
from typing import Any

import numpy as np

from worker.models import ArtifactIO, ModelConfig, PreprocessConfig, AuxilaryData, create_model
from ml.scripts.registry import create_trainer


def main() -> None:
    return


if __name__ == "__main__":
    main()
